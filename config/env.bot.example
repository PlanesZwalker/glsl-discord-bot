# Discord Bot Configuration
# Obtenez ces valeurs depuis https://discord.com/developers/applications
# Copiez ce fichier vers .env à la racine du projet

# Token du bot Discord (Bot > Token)
DISCORD_TOKEN=your_discord_bot_token_here

# Client ID de l'application Discord (General Information > Application ID)
DISCORD_CLIENT_ID=your_discord_client_id_here

# Public Key de l'application Discord (General Information > Public Key)
# Utilisé pour vérifier les signatures des interactions Discord
DISCORD_PUBLIC_KEY=your_discord_public_key_here

# Port pour le serveur HTTP du bot (optionnel, défaut: 8080)
PORT=8080

# Clé API pour sécuriser les endpoints du bot (optionnel mais recommandé)
# Générez une clé aléatoire sécurisée (ex: openssl rand -hex 32)
BOT_API_KEY=your_random_api_key_here

# Configuration WebGL (optionnel)
WEBGL_CANVAS_WIDTH=800
WEBGL_CANVAS_HEIGHT=600
WEBGL_FRAME_RATE=30
WEBGL_DURATION=3

# Configuration Base de Données (optionnel)
DATABASE_PATH=./data/shaders.db

# Configuration Puppeteer (optionnel)
PUPPETEER_TIMEOUT=30000
PUPPETEER_HEADLESS=true

# Configuration Sécurité (optionnel)
MAX_SHADER_LENGTH=10000
MAX_COMPILATION_TIME=30000

# Configuration Logs (optionnel)
LOG_LEVEL=info
LOG_MAX_FILES=5
LOG_MAX_SIZE=10m

