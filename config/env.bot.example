# Discord Bot Configuration
# Obtenez ces valeurs depuis https://discord.com/developers/applications
# Copiez ce fichier vers .env à la racine du projet

# Token du bot Discord (Bot > Token)
DISCORD_TOKEN=your_discord_bot_token_here

# Client ID de l'application Discord (General Information > Application ID)
DISCORD_CLIENT_ID=your_discord_client_id_here

# Public Key de l'application Discord (General Information > Public Key)
# Utilisé pour vérifier les signatures des interactions Discord
DISCORD_PUBLIC_KEY=your_discord_public_key_here

# Port pour le serveur HTTP du bot (optionnel, défaut: 8080)
PORT=8080

# Clé API pour sécuriser les endpoints du bot (optionnel mais recommandé)
# Générez une clé aléatoire sécurisée (ex: openssl rand -hex 32)
BOT_API_KEY=your_random_api_key_here

# Configuration WebGL (optionnel)
WEBGL_CANVAS_WIDTH=800
WEBGL_CANVAS_HEIGHT=600
WEBGL_FRAME_RATE=30
WEBGL_DURATION=3

# Configuration Base de Données (optionnel)
DATABASE_PATH=./data/shaders.db

# Configuration Puppeteer (optionnel)
PUPPETEER_TIMEOUT=30000
PUPPETEER_HEADLESS=true

# Configuration Sécurité (optionnel)
MAX_SHADER_LENGTH=10000
MAX_COMPILATION_TIME=30000

# Configuration Logs (optionnel)
LOG_LEVEL=info
LOG_MAX_FILES=5
LOG_MAX_SIZE=10m

# ============================================
# STRIPE CONFIGURATION (Monétisation)
# ============================================
# Obtenez ces valeurs depuis https://dashboard.stripe.com

# Clé secrète Stripe (API Keys > Secret key)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here

# Webhook secret Stripe (Developers > Webhooks > Signing secret)
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Price IDs Stripe pour les plans (Products > Pricing)
STRIPE_PRICE_ID_PRO=price_your_pro_price_id_here
STRIPE_PRICE_ID_STUDIO=price_your_studio_price_id_here

# ============================================
# CLEANUP CONFIGURATION
# ============================================

# Intervalle de nettoyage automatique en heures (optionnel, défaut: 24)
# Le système nettoie automatiquement les shaders des utilisateurs gratuits > 7 jours
CLEANUP_INTERVAL_HOURS=24

# ============================================
# COMPILATION CONFIGURATION
# ============================================

# Nombre maximum d'instances de navigateur en parallèle (optionnel, défaut: 2)
MAX_BROWSER_INSTANCES=2

# Timeout pour les compilations en millisecondes (optionnel, défaut: 30000)
COMPILATION_TIMEOUT=30000

# Nombre maximum de compilations simultanées (optionnel, défaut: 2)
MAX_CONCURRENT_COMPILATIONS=2

# ============================================
# REDIS CONFIGURATION (Optionnel)
# ============================================
# Pour le rate limiting avancé et la queue (optionnel)

# URL de connexion Redis (optionnel)
# Exemple: redis://localhost:6379
REDIS_URL=

# ============================================
# ENVIRONMENT
# ============================================

# Environnement d'exécution (optionnel, défaut: development)
# Options: development, production, test
NODE_ENV=development

# URL de base de l'application web (pour les liens dans les messages)
WEB_URL=https://your-app-url.com

