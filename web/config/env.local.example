# Next.js Web Application Environment Variables
# Copiez ce fichier vers .env.local dans le dossier web/

# URL de base de l'application (pour Vercel, utilisez votre URL de déploiement)
# Exemple local: http://localhost:3000
# Exemple Vercel: https://your-app.vercel.app
NEXTAUTH_URL=http://localhost:3000

# Secret pour NextAuth.js (générez une clé aléatoire sécurisée)
# Exemple: openssl rand -base64 32
NEXTAUTH_SECRET=your_nextauth_secret_here

# Discord OAuth2 Configuration
# Obtenez ces valeurs depuis https://discord.com/developers/applications
# Créez une OAuth2 application et ajoutez les redirect URIs:
# - http://localhost:3000/api/auth/callback/discord (pour le développement)
# - https://your-app.vercel.app/api/auth/callback/discord (pour la production)

# Client ID de l'application Discord OAuth2 (General Information > Application ID)
DISCORD_CLIENT_ID=your_discord_client_id_here

# Client Secret de l'application Discord OAuth2 (OAuth2 > Client Secret)
DISCORD_CLIENT_SECRET=your_discord_client_secret_here

# URL publique du Client ID (pour les liens d'invitation du bot)
NEXT_PUBLIC_DISCORD_CLIENT_ID=your_discord_client_id_here

# URL d'invitation personnalisée du bot (optionnel)
# Si non défini, sera généré automatiquement avec NEXT_PUBLIC_DISCORD_CLIENT_ID
NEXT_PUBLIC_BOT_INVITE_URL=

# Configuration de l'API du bot (pour la production)
# URL de l'API du bot (ex: https://your-bot.onrender.com)
BOT_API_URL=

# Clé API pour authentifier les requêtes vers l'API du bot
# Doit correspondre à BOT_API_KEY dans le bot
BOT_API_KEY=

# Chemin vers la base de données SQLite (pour le développement local uniquement)
# En production sur Vercel, utilisez BOT_API_URL à la place
DATABASE_PATH=

